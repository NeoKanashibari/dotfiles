#
# ~/.bash_profile
#

[ -z "$SSH_AUTH_SOCK" ] && eval "$(ssh-agent -s)" 
[ -f "~/.ssh/id_rsa" ] && ssh-add ~/.ssh/id_rsa

[[ -f ~/.bashrc ]] && . ~/.bashrc
[[ -z $DISPLAY && $(tty) == "/dev/tty1" ]] && startx || true
